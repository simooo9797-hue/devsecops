apiVersion: v1
kind: Service
metadata:
  name: devsecops-demo-app-service
spec:
  type: NodePort
  selector:
    app: devsecops-demo-app
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30080